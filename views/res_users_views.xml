<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_users_form_smart_access" model="ir.ui.view">
        <field name="name">res.users.form.smart.access</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Smart Access Profiles" name="smart_access_profiles">
                    <group>
                        <group string="Assigned Security Profiles">
                            <field name="access_management_ids" widget="many2many_tags" placeholder="Assign profiles to user..."/>
                        </group>
                        <group string="Active Security Status">
                            <field name="is_readonly_user" readonly="1"/>
                            <field name="is_dev_mode_disabled" readonly="1"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
